export interface CategorySelectModel {
  id: number;
  name: string;
  parentId?: null;
  parentName?: string;
}

export interface CategoryRegistertModel {
  id: number;
  name: string;
  parentId?: null;
}
